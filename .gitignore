# Virtual environment
Lib/
Scripts/
Include/
pyvenv.cfg

# Secrets
.env
.env.bak
*.env.bak

# Backups / scratch copies
*.bak
* - Copy.py
* - Copy.*

# Media assets (logos/music kept local, not committed)
media/

# Refresh-script output -- entirely machine-generated (regenerated by the
# refresh_*.py scripts / the server's own refresh timers), not source. Was
# tracked before, which caused `git pull` on the server to fail with local-
# changes-would-be-overwritten whenever the server's own refresh job had
# run since the last pull.
data/*.json

# Python
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store
